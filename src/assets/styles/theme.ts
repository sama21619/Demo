
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    typography: {
      
        fontSize: 16,
        h1: {
          fontSize: '2rem', 
        },
        h2: {
          fontSize: '1.5rem', 
      },}
    });

export default theme;
